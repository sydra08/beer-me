<header class="align-center">
	<h2>Categories</h2>
</header>

<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %> <%= "(#{category.beers.count})" if category.beers %></li>
  <% end %>
</ul>
